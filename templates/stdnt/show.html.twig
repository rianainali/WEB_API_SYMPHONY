{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ student.name }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ student.level }}</td>
            </tr>
            <tr>
                <th>Stream</th>
                <td>{{ student.stream }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stdnt_index') }}"><button type="button" class="btn btn-primary"> back to list</button></a>
    <a href="{{ path('app_stdnt_edit', {'id': administrator.id}) }}"><button type="button" class="btn btn-primary">edit</button></a>

    {{ include('stdnt/_delete_form.html.twig') }}
{% endblock %}
