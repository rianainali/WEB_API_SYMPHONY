<form method="post" action="{{ path('app_admin_delete', {'id': administrator.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administrator.id) }}">
    <button class="btn btn-primary mt-2">Delete</button>
</form>
